[package]
name = "sum_tree"
version = "0.1.0"
edition.workspace = true
publish = false
license = "Apache-2.0"
description = "A sum tree data structure, a concurrency-friendly B-tree"

[lints]
workspace = true

[lib]
path = "src/sum_tree.rs"
doctest = false

# SEEKER PATCH: dropped optional `proptest` dependency (git-pinned fork
# upstream) and the proptest-based property_test module; test-support is now
# an empty feature.
[dependencies]
heapless.workspace = true
rayon.workspace = true
log.workspace = true
ztracing.workspace = true
tracing.workspace = true

[dev-dependencies]
ctor.workspace = true
rand.workspace = true
zlog.workspace = true


[package.metadata.cargo-machete]
ignored = ["tracing"]

[features]
test-support = []
